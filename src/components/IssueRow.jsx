import React from 'react'

const IssueRow = ({issue}) => {

    const {issueTitle, issueDescription, issueStatus, issuePriority, issueAssignedTo} = issue;

  return (
    <div>IssueRow</div>
  )
}

export default IssueRow